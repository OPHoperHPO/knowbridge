# --- Docker specific files ---
# Ignore Docker related files themselves
Dockerfile
docker-compose.yml
.dockerignore

# --- Version Control ---
# Ignore the Git repository directory and related files
.git/
.gitignore
# Add .hg/, .svn/ etc. if you use other version control systems

# --- Build Artifacts ---
# Ignore the typical CMake build directory
build/
# Ignore potential local install directories if you create them
install/
# Ignore compiled object files, libraries, and executables if built locally
*.o
*.a
*.so
*.dylib
*.dll
*.exe
# Explicitly ignore the executable name if ever built outside 'build/'
kde-llm-enhancer

# --- CMake specific cache/files ---
# These are usually inside build/, but good practice to list explicitly
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
Makefile
compile_commands.json

# --- IDE / Editor Configuration ---
# Ignore common IDE/editor specific directories and files
.vscode/
.idea/
*.code-workspace
.settings/
.project
.classpath
*.creator.user

# --- Temporary & Backup Files ---
# Ignore files typically generated by editors or tools
*~
*.swp
*.swo
*.bak
*.tmp
*.orig
*.rej

# --- Log Files ---
# Ignore any local log files
*.log

# --- Generated Source Files (If they somehow end up in the source tree) ---
# KCFG generated files are typically placed in the build directory by the provided CMakeLists,
# so explicitly ignoring them here might not be strictly necessary unless your setup differs.
# config/settings.h
# config/settings.cpp
# config/kde-llm-enhancer.kcfgc

# --- OS Specific Files ---
# Ignore metadata files from macOS and Windows
.DS_Store
Thumbs.db
dist/
cmake-build-debug/

# --- Python Virtual Environments (if used for any helper scripts) ---
.venv/
venv/
env/
*/env/
*/venv/
*.pyc
__pycache__/

# --- User runtime config file (should not be part of the source) ---
# This is mounted via docker-compose, don't copy it from source context.
# Add the actual name of your runtime config file if it's different
kdellmenhanrcerrc
tools/src.txt
cmake-build-*